# AIM v7.0 — Гибридная медицинская AI-экосистема

**Версия:** 7.0.0  
**Дата:** 2026-04-15  
**Статус:** Активная разработка

---

## 1. Миссия

AIM (Assistant of Integrative Medicine) — клинический AI-ассистент для Dr. Jaba Tkemaladze. Поддерживает 9 языков, работает через гибридный LLM-роутер, не зависит от локальных моделей.

**Пользователи:** врач · пациент · клиника

---

## 2. Ключевое изменение v7.0

| Компонент | v6.0 | v7.0 |
|-----------|------|------|
| LLM | Ollama / llama3.2 (локально) | Гибридный API-роутер |
| Мозг | Одна модель | KIMI + Qwen + DeepSeek |
| Языки | 7 (ООН-6 + KA) | 9 (ООН-6 + KA + KZ + DA) |
| Контекст | ~4k токенов | До 128k (KIMI) |
| Автономность | Нет | Agent loop |

---

## 3. Гибридный LLM-роутер

Центральный компонент системы — `llm.py`. Роутер выбирает модель по типу задачи:

```
Входящая задача
      │
      ▼
 [Router в llm.py]
      │
      ├─ длинный документ (PDF, выписка, >16k токенов) ──► KIMI
      │
      ├─ язык AR / ZH / KA / KZ / DA ──────────────────► Qwen
      │
      ├─ диагностика / рассуждение / анализ ───────────► DeepSeek-reasoner
      │
      ├─ быстрый простой ответ (<3k токенов) ──────────► Groq (Llama 3.3 70B)
      │
      └─ стандартный RU / EN / FR / ES ────────────────► DeepSeek-chat
```

### Модели

| Провайдер | Модель | Сильная сторона | Endpoint |
|-----------|--------|-----------------|----------|
| Groq | llama-3.3-70b-versatile | **Ультра-быстро** (<1 сек), простые запросы | api.groq.com |
| DeepSeek | deepseek-chat | Быстро, RU/EN, код | api.deepseek.com |
| DeepSeek | deepseek-reasoner | Диагностика, цепочки рассуждений | api.deepseek.com |
| KIMI | moonshot-v1-128k | Длинный контекст, PDF-анализ | api.moonshot.cn |
| Qwen | qwen-turbo | Многоязычность, AR/ZH/KA/KZ/DA | dashscope.aliyuncs.com |
| Qwen | qwen-max | Сложный многоязычный анализ | dashscope.aliyuncs.com |

### Fallback-цепочка

```
KIMI недоступен → DeepSeek-chat (с предупреждением об обрезке контекста)
Qwen недоступен → DeepSeek-chat (только для EN/RU)
DeepSeek недоступен → Qwen-turbo
Все недоступны → cached response / error
```

---

## 4. Языки

| Код | Язык | Движок | Контекст |
|-----|------|--------|----------|
| ru | Русский | DeepSeek | Основной |
| en | English | DeepSeek / KIMI | Основной |
| fr | Français | DeepSeek | ООН |
| es | Español | DeepSeek | ООН |
| ar | العربية | Qwen | ООН |
| zh | 中文 | Qwen | ООН |
| ka | ქართული | Qwen | Основной (практика) |
| kz | Қазақша | Qwen | ЦА-аудитория |
| da | Dansk | Qwen | Скандинавия |

Детектирование языка — автоматически по тексту + явный параметр `lang`.

---

## 5. Архитектура системы

```
┌─────────────────────────────────────────┐
│              AIM v7.0                   │
├─────────────────────────────────────────┤
│                                         │
│  medical_system.py  ←──  Точка входа   │
│         │                               │
│         ▼                               │
│    [Agent Loop]                         │
│         │                               │
│    ┌────▼────┐                          │
│    │ llm.py  │  ← Гибридный роутер      │
│    │ Router  │                          │
│    └────┬────┘                          │
│         │                               │
│   ┌─────┼─────┐                        │
│   ▼     ▼     ▼                        │
│ KIMI  Qwen  DeepSeek                   │
│         │                               │
│    ┌────▼────┐                          │
│    │  db.py  │  ← SQLite               │
│    └────┬────┘                          │
│         │                               │
│    Patients/  ←  Данные пациентов       │
└─────────────────────────────────────────┘
```

### Модули

| Файл | Назначение |
|------|-----------|
| `medical_system.py` | Точка входа, agent loop, CLI |
| `llm.py` | Гибридный роутер: KIMI + Qwen + DeepSeek |
| `config.py` | Конфигурация, ключи, пути, модели |
| `db.py` | SQLite: пациенты, сессии, кэш |
| `i18n.py` | 9 языков: строки UI + системные промпты |
| `router.py` | Логика выбора модели, task classifier |
| `agents/doctor.py` | Агент врача: диагностика, назначения |
| `agents/intake.py` | Агент ввода: OCR, PDF, анализы |
| `agents/lang.py` | Языковой агент: детектор + переводчик |

---

## 6. Agent Loop

```
Запрос (текст / файл / команда)
         │
         ▼
   [Классификатор]
    ├─ Тип: диагностика / анализ / перевод / вопрос / файл
    └─ Язык: автоопределение
         │
         ▼
   [Выбор агента]
    ├─ doctor.py — медицинские вопросы
    ├─ intake.py — файлы и анализы
    └─ lang.py  — перевод / смена языка
         │
         ▼
   [Router → LLM]
         │
         ▼
   [Ответ + сохранение в db.py]
```

---

## 7. Данные пациентов

- `Patients/` — папки формата `SURNAME_NAME_YYYY_MM_DD/`
- `Patients/INBOX/` — новые файлы (автоматический intake)
- Данные не коммитятся, не передаются третьим сторонам
- SQLite (`aim.db`) — индекс и кэш; реальные данные в `Patients/`

---

## 8. Переменные окружения (`~/.aim_env`)

```
DEEPSEEK_API_KEY=...        # уже есть
KIMI_API_KEY=...            # добавить → api.moonshot.cn
QWEN_API_KEY=...            # добавить → dashscope.aliyuncs.com
GROQ_API_KEY=...            # добавить → console.groq.com (бесплатно)
TELEGRAM_BOT_TOKEN=...      # есть
TELEGRAM_ALLOWED_ID=...     # есть
```

---

## 9. Принципы разработки

1. **Роутер — не модель делает магию** — правильный выбор модели важнее самой модели
2. **Пациенты неприкосновенны** — `Patients/` никогда не в git, никогда без явной команды
3. **Ключи не в коде** — только `~/.aim_env`
4. **9 языков везде** — i18n с первого дня, не потом
5. **Fallback всегда** — система работает даже при недоступности одного провайдера
6. **Один вход** — `medical_system.py`, всё остальное — модули

---

## 10. Дорожная карта v7.0

| Этап | Задача | Статус |
|------|--------|--------|
| 1 | CONCEPT.md | ✅ |
| 2 | config.py + llm.py (роутер) | 🔄 |
| 3 | i18n.py (9 языков) | ⏳ |
| 4 | db.py | ⏳ |
| 5 | agents/ | ⏳ |
| 6 | medical_system.py | ⏳ |
| 7 | Тестирование роутера | ⏳ |
| 8 | OCR + PDF pipeline | ⏳ |
